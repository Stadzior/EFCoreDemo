<UserControl
    x:Class="EFCoreDemo.View.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="800"
    Height="450"
    DataContext="{Binding MainViewModel, Source={x:Static Application.Current}}"
    mc:Ignorable="d">
    <Grid>
        <ListView ItemsSource="{Binding Path=Foos, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding Path=Id}"
                        Header="Id" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding Path=Name}"
                        Header="Name" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding Path=Bar.Name}"
                        Header="Bar" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
